-- | All type-classes, types and datatypes needed to build a new backend
module Graphics.Gloss.Interface.Backend
	(module Graphics.Gloss.Internals.Interface.Backend.Types)
where

import Graphics.Gloss.Internals.Interface.Backend.Types
	(Backend(..),Callback(..),Key(..),MouseButton(..),KeyState(..),SpecialKey(..),Modifiers(..))
